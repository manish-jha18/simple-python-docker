version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    # Uncomment to mount local code for rapid development:
    # volumes:
    #   - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
